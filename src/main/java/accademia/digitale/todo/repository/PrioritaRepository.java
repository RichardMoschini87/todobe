package accademia.digitale.todo.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import accademia.digitale.todo.models.Priorita;

public interface PrioritaRepository extends JpaRepository<Priorita, Integer>{

} 
